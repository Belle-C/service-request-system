services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: service_request_system
      POSTGRES_USER: service_request
      POSTGRES_PASSWORD: service_request
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

