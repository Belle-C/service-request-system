export default function MyRequestsPage() {
  return (
    <main className="mx-auto max-w-6xl px-6 py-10">
      <h1 className="text-3xl font-semibold text-[var(--primary)]">My Requests</h1>
      <p className="mt-2 text-[var(--muted)]">
        Request list, filters, search, view, and amend actions will live here.
      </p>
    </main>
  );
}

