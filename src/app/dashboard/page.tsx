export default function DashboardPage() {
  return (
    <main className="mx-auto max-w-6xl px-6 py-10">
      <h1 className="text-3xl font-semibold text-[var(--primary)]">Dashboard</h1>
      <p className="mt-2 text-[var(--muted)]">
        The backend will map this page to the correct module dashboard by user role.
      </p>
    </main>
  );
}

